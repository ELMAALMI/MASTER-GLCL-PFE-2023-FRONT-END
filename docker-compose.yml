version: '3'
services:
    front-end:
        build:
            context: .
        ports:
            - '3000:3000'
        environment:
            - ENV=dev
        volumes:
            - ./:/usr/src/app/
